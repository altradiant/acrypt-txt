## AES (Advanced Encryption Standard)

### Введение

AES (Advanced Encryption Standard) — это симметричный блочный шифр, принятый в качестве стандарта шифрования данных в США в 2001 году. AES заменил устаревший DES (Data Encryption Standard) и стал основным алгоритмом для защиты конфиденциальной информации. AES шифрует данные блоками фиксированной длины (128 бит) с использованием ключей длиной 128, 192 или 256 бит.

### Основные характеристики

- **Тип шифра**: Симметричный блочный шифр.
- **Длина блока**: 128 бит.
- **Длина ключа**: 128, 192 или 256 бит.
- **Количество раундов**: Зависит от длины ключа:
  - 10 раундов для ключа 128 бит.
  - 12 раундов для ключа 192 бит.
  - 14 раундов для ключа 256 бит.
- **Используемые операции**: Подстановки (S-блоки), перестановки, смешивание строк и столбцов, XOR с ключом.

**В программной реализации будет использоваться ключ 256 бит.**

Не забываем, что:

Первоначальный ключ в программных реализациях преобразуется по хэш-функции; выходной результат шифрования кодируется с помощью base64; соответственно, при расшифровке сначала происходит раскодировка base64.

IV и nonce также крепятся к началу шифротекста (IV или nonce в зависимости от режима: CBC или CTR)

*Во многом, AES похож на DES, поскольку последний является его прямым предшественником.*

### Режим CBC (Cipher Block Chaining)

#### Описание

В режиме CBC каждый блок открытого текста XOR-ится с предыдущим блоком шифротекста перед шифрованием. Это обеспечивает зависимость между блоками, что делает шифрование более безопасным, так как одинаковые блоки открытого текста будут давать разные блоки шифротекста.

#### Принцип работы

1. **Инициализация**: Используется случайный вектор инициализации (IV), который XOR-ится с первым блоком открытого текста.
2. **Шифрование**:
   - Первый блок открытого текста XOR-ится с IV, затем шифруется с использованием AES.
   - Каждый последующий блок открытого текста XOR-ится с предыдущим блоком шифротекста перед шифрованием.
3. **Дешифрование**:
   - Каждый блок шифротекста дешифруется с использованием AES.
   - Результат XOR-ится с предыдущим блоком шифротекста для получения открытого текста.

### Режим CTR (Counter)

#### Описание

В режиме CTR используется счетчик (counter), который шифруется с использованием AES, а затем результат XOR-ится с блоком открытого текста. Этот режим позволяет обрабатывать блоки независимо, что делает его более эффективным для параллельной обработки.

#### Принцип работы

1. **Инициализация**: Используется счетчик (counter), который увеличивается для каждого блока.
2. **Шифрование**:
   - Счетчик шифруется с использованием AES.
   - Результат шифрования XOR-ится с блоком открытого текста для получения шифротекста.
3. **Дешифрование**:
   - Тот же процесс используется для дешифрования, так как шифрование и дешифрование в режиме CTR идентичны.

На этот раздел отведено три задания.

*Оригинально, этот раздел являлся заключительным, но если вы хотите, то можете изучить ещё два шифра. Я не буду против.* ;P